

{#
| Variable        | Type      | Description                                                      |
|-----------------|-----------|------------------------------------------------------------------|
| products        | Product[] | List of products                                                 |
| title           | string    | Section title                                                    |
| display_all_url | ?string   | If it's existed, mean should show load more link to this url     |
| position        | Int       | Sorting number start from zero                                   |
#}



<section id="best-offers-{{ position }}-slider" class="s-block s-block--best-offers container overflow-hidden">
  {% if products|length %}
    <salla-products-slider
            source="selected"
            limit="{{ limit }}"
            autoplay
            source-value="[{{ products|map((product) => product.id)|join(',') }}]"
            display-all-url="{{display_all_url}}"
            block-title="{{ title }}"
            block-subTitle="{{ sub_title|raw }}"
            slider-id="slider-with-bg-{{ position }}"
    ></salla-products-slider>
  {% endif %}

  
</section>


<style>
  .product{
    display: flex; 
overflow: hidden; 
position: relative; 
flex-direction: column; 
justify-content: space-between; 
border-radius: 0.75rem; 
border-width: 1px; 
border-color: #F3F4F6; 
width: 100%; 
height: 100%; 
background-color: #ffffff; 
box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); 


  }
  .product-block{
    display: flex;
    justify-content: space-between;
    word-wrap: break-word;

  }
 


.product-block__thumb  {
  display: flex; 
  overflow: hidden; 
  position: relative; 
  margin-left: 0.5rem;
  margin-right: 0.5rem; 
  margin-top: 0.75rem; 
  border-radius: 0.75rem; 
  
  
}

.product-block__thumb  img{
  width: 150%;
  height: 200;
  object-fit: cover;
  
  
}

.product-block__thumb .badge {
  position: absolute; 
top: 0.75rem; 
right: 0.75rem; 
display: block; 
padding-top: 0.25rem;
padding-bottom: 0.25rem; 
padding-left: 0.75rem;
padding-right: 0.75rem; 
margin-top: 0.5rem;
margin-bottom: 0.5rem; 
border-radius: 0.375rem; 
width: fit-content; 
font-size: 0.80rem;
line-height: 1rem; 
font-weight: 700; 
background-color: #F3F4F6; 


}
.product-block__info{
  display: flex; 
flex-direction: column; 
justify-content: space-between; 
height: min-content; 

padding-left: 1rem;
padding-right: 1rem; 
flex-direction: column; 



}
.product-main-info{
display: flex; 
flex-direction: column; 
gap: 0.5rem; 

}

.price-wrapper{
margin-top: 0.25rem; 
margin-bottom: 0.75rem; 
align-items: center; 
width: 100%; 
text-align: center; 









}

.product-title-section .product-title{

  display: flex; 
margin-top: 1rem; 
flex-direction: column; 





}
.product-bottom-details{
  display: flex; 
padding-top: 0.5rem;
padding-bottom: 0.5rem; 
padding-left: 1.5rem;
padding-right: 1.5rem; 
margin-top: 1rem; 
flex-direction: column; 
justify-content: space-between; 
align-items: center; 
height: min-content; 
text-align: center; 


}
.btn--add-product{
  height:52px;
  padding-top:8px;
  padding-bottom:8px;
  transition:all .3s ease;
  border-radius:4px;
  border-width:1px;
  background-color:var(--color-primary);
  
}

.product .btn--add-product:hover{
 }
 
.product .btn--add-product .s-button-btn {
  line-height:none;
  color:#ffffff;
  transition:all .3s ease}

.product .btn--add-product .s-button-btn:hover{color:#fff}

.product .product-card-btn--wishlist{
  padding: 10px;
  display: inline-block;
  outline: 0;
  border: 0;
  margin: -1px;
  border-radius: 2px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: white;
  cursor: pointer;
  width: 22%;
  background-color:var(--color-primary);

}

.product .product-card-btn--wishlist:hover {
  background-color:var(--color-primary);
    transition: all .4s ease-in-out;
  }
  .s-products-list-wrapper .s-products-list-vertical-cards {
 background-color: red;
  
}


button,
input,
optgroup,
select,
textarea {
    font-feature-settings: inherit;
    color: inherit;
    font-family: inherit;
    font-size: 100%;
    font-variation-settings: inherit;
    font-weight: inherit;
    letter-spacing: inherit;
    line-height: inherit;
    margin: 0;
    padding: 0;
}
button,
select {
    text-transform: none;
}
button,
input:where([type="button"]),
input:where([type="reset"]),
input:where([type="submit"]) {
    -webkit-appearance: button;
    background-color: initial;
    background-image: none;
}
:-moz-focusring {
    outline: auto;
}
:-moz-ui-invalid {
    box-shadow: none;
}
progress {
    vertical-align: initial;
}
::-webkit-inner-spin-button,
::-webkit-outer-spin-button {
    height: auto;
}
[type="search"] {
    -webkit-appearance: textfield;
    outline-offset: -2px;
}
::-webkit-search-decoration {
    -webkit-appearance: none;
}
::-webkit-file-upload-button {
    -webkit-appearance: button;
    font: inherit;
}
summary {
    display: list-item;
}
blockquote,
dd,
dl,
figure,
h1,
h2,
h3,
h4,
h5,
h6,
hr,
p,
pre {
    margin: 0;
}
fieldset {
    margin: 0;
}
fieldset,
legend {
    padding: 0;
}
menu,
ol,
ul {
    list-style: none;
    margin: 0;
    padding: 0;
}
dialog {
    padding: 0;
}
textarea {
    resize: vertical;
}
input::placeholder,
textarea::placeholder {
    color: #9ca3af;
    opacity: 1;
}
[role="button"],
button {
    cursor: pointer;
}
:disabled {
    cursor: default;
}
</style>

