
<section class="relative h-screen flex flex-col items-center justify-center text-center text-white ">
    <div class="video-docker absolute top-0 left-0 w-full h-full overflow-hidden">
        {% if component.videotoimg    %}
        <img class="lazy w-full h-full object-cover object-center min-h-[250px] max-h-[350px] lg:max-h-[700px] loaded" data-src="{{component.photo}}"
         data-ll-status="loaded">
            {% else %}

            <video class="min-w-full min-h-full absolute "
            src="{{component.url}}"
            type="video/mp4" autoplay muted loop></video>
            {% endif %}

    </div>

  
    <div class="video-content space-y-2 z-10">
        <h1 class="font-light text-6xl">{{component.title}}</h1>
        <h3 class="font-light text-3xl">{{component.desc}}</h3>
    </div>

    <div class="mt-4 md:mt-4 flex md:justify-start items-center gap-x-6">
        <button class="button-custom md:text-[20px] text-[17px] flex gap-4 justify-center items-center shadow rounded-[0.25rem] h-12 w-full py-2 px-8 duration-100" href="{{component.link}}" type="button" >
        <span class="text"> {{component.titleButton}}</span>

      <i class="arrow-icon sicon-arrow-up-left"></i>
      </button>
      </div>
</section>

<style>
    .video-docker video {
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    .video-docker::after {
        content: "";
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        background: rgba(0, 0, 0, 0.6);
        z-index: 1;
    }
</style>