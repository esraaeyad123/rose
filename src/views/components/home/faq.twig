
<section class=" bg-white   relative py-8 lg:py-16 overflow-hidden">
   
  
  <div class="container relative px-6 lg:px-8 md:flex">
    <div class="flex flex-col  basis-1/2">
        <p class="inline-block font-semibold text-primary mb-4">{{component.title}}</p>
        <p class="sm:text-4xl text-3xl font-extrabold text-base-content">{{component.description}}</p>
    </div>
    <ul class="basis-1/2">
        {% for item in component.faq %}

        <div class="py-5">
            <details class="group">
                <summary class="flex cursor-pointer list-none items-center justify-between font-medium">
                    <span class="text-base font-semibold leading-7">        {{item.item_1}}
                    </span>
                    <span class="transition group-open:rotate-180">
                            <svg fill="none" height="24" shape-rendering="geometricPrecision"
                                stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"
                                stroke-width="1.5" viewBox="0 0 24 24" width="24">
                                <path d="M6 9l6 6 6-6"></path>
                            </svg>
                        </span>
                </summary>
                <p class="text-base leading-7 pt-2 group-open:animate-fadeIn mt-3 text-neutral-600">{{item.item}}
                </p>
            </details>
        </div>
        {% endfor %}
    </ul>
</div>
</section>
