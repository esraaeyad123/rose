{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int     |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}
<div class="promo-banner" id="promoBanner">
  <div class="banner-content">
      <p class="promo-text">🌟 عرض خاص: احصل على 20% خصم على أول طلب لك! 🌟</p>
  </div>
  <button class="close-button" onclick="closeBanner()">✖</button>
</div>
<div class="art-top-navbar store-header">
  <div class="top-navbar bg-[var(--color-upper-background)] text-[var(--color-upper-text)] h-full w-full">
    <div class="flex gap-4 md:flex-row flex-col md:justify-between justify-center items-center w-full lg:px-10 md:px-5 px-2">
      <div class="flex flex-wrap md:gap-x-4 md:gap-0 gap-2 items-center text-[16px] text-[var(--color-upper-text)]">
        <span class="uppercase ltr:tracking-[2px]">خصم حتى 40%</span>
        <a href="" class="md:block hidden relative text-[16px] ltr:tracking-[2px] uppercase after:absolute after:w-full after:h-[1px] after:bg-[var(--color-upper-text)] after:bottom-[0px] ltr:after:left-0 rtl:after:right-0 hover:after:w-0 after:duration-500">تسوق الان</a>
      </div>
    
      <div class="art-countdown-nav">
        <salla-count-down class="salla-count-down" data-end-time="2024-11-30" days hours minutes>
         
        </salla-count-down>
      </div>
      
      
      
    </div>
  </div>
</div>



{% set important_links = theme.settings.get('important_links') %}
<header class="custom-store-header store-header dark:bg-[var(--color-dark-primary)] dark:text-white">
  {# Top Nav #}
  <div class="custom-top-navbar top-navbar relative">
    <div class="container mx-auto">
    <div class="grid lg:grid-cols-12 grid-cols-1 lg:items-center lg:justify-between lg:py-0 py-2 xl:px-20 lg:px-6">
    <div class="w-full col-span-6 flex lg:flex-row flex-col items-center xl:gap-[3.5rem] lg:gap-10 gap-4">
    <div class="flex lg:items-start lg:justify-start items-center justify-between lg:w-auto w-full">
    <div class="flex items-center justify-start gap-2">
      <a class="lg:hidden mburger mburger--collapse leading-none rtl:ml-4 ltr:mr-4" href="#mobile-menu" aria-label="Open-menu">
        <i class="sicon-menu text-primary text-2xl"></i>
      </a>
      <a class="navbar-brand" href="{{ store.url }}">
          <img fetchpriority="high" width="100%" height="100%" loading="eager" src="{{ store.logo }}" alt="{{ store.name }} logo">
          {% if is_page('index') %}
            <h1 class="sr-only">{{ store.name }}</h1>
          {% else %}
            <h2 class="sr-only">{{ store.name }}</h2>
          {% endif %}
      </a>
  
    </div>
    <div class="flex items-center justify-start">
    <div class="flex lg:hidden justify-center bg-transparent items-center relative">
      {% if user.type=='user' %}
      <salla-user-menu avatar-only show-header></salla-user-menu>
    {% else %}
        <button class="header-btn flex justify-center items-center gap-2 relative px-3" aria-label="user-icon" onclick="salla.event.dispatch('login::open')">
          <i class="sicon-user">
          </i>
        </button>
    {% endif %}
    </div>
    <div class="lg:hidden flex header-buttons mx-3">
      {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
      <button type="button" onclick="salla.event.dispatch('localization::open')" class="btn--rounded-gray basis-0">
          <span class="flag iti__flag iti__{{ user.language.country_code }} rounded-sm"></span>
          <span class="mx-1.5">|</span> <span>{{ currency.symbol }}</span>
      </button>
      <salla-localization-modal></salla-localization-modal>
  {% endif %}
    </div>
    <div class="flex lg:hidden items-center justify-end">
      <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4">
        <i slot="icon" class="header-btn__icon  icon sicon-shopping-basket"></i>
      </salla-cart-summary>
    </div>
    </div>
    </div>
    <div class="header-search flex-1 w-full lg:w-auto">
      <salla-search inline oval height="46"></salla-search>
  
    </div>
    </div>
  
  
  
  
  
  
  
  
  
  
    <div class="lg:flex lg:col-start-7 col-end-13 hidden justify-end items-center w-full h-full">
    <div class="lg:flex hidden justify-center bg-transparent items-center relative ltr:border-r rtl:border-l border-[#deddd9] dark:border-slate-300">
    {% if user.type=='user' %}
    <salla-user-menu avatar-only show-header></salla-user-menu>
  {% else %}
      <button class=" header-btn flex justify-center items-center gap-2 relative px-3" aria-label="user-icon" onclick="salla.event.dispatch('login::open')">
        <i class="sicon-user">
        </i>
      </button>
  {% endif %}
    </div>
    <div class="lg:flex hidden header-buttons xl:px-3 lg:pr-3">
      {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
      <button type="button" onclick="salla.event.dispatch('localization::open')" class="btn--rounded-gray basis-0">
          <span class="flag iti__flag iti__{{ user.language.country_code }} rounded-sm"></span>
          <span class="mx-1.5">|</span> <span>{{ currency.symbol }}</span>
      </button>
      <salla-localization-modal></salla-localization-modal>
  {% endif %}
    </div>
    </div>
    </div>
    </div>
    </div>

  {# Main Nav #}
  <div id="mainnav" class="custom-main-nav main-nav-container border-b border-gray-300 bg-white dark:border-slate-600 dark:bg-[var(--color-dark-secondary)] lg:h-auto fixed-pinned animated fixed-header">
    <div class="inner bg-inherit">
    <div class="container mx-auto ">
    <div class="flex flex-wrap items-center justify-between relative xl:px-20 lg:px-6">
    <div class="flex items-center  ">
    <!--original menu location-->
    <custom-main-menu  ></custom-main-menu>

  
    </div>
    <div class="lg:flex gap-4 hidden items-center justify-end py-4">
    <span class="test-sm font-semibold">سلة المشتريات</span>
    <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4">
      <i slot="icon" class="header-btn__icon icon sicon-shopping-bag"></i>
    </salla-cart-summary>
    </div>
    </div>
    </div>
    </div>
    </div>
 
   

</header>
{% if store.scope %}
    <salla-scopes selection="{{ store.scope.display_as == 'popup' ? 'mandatory' : 'optional' }}"></salla-scopes>
{% endif %}

<!-- إعلان ترويجي -->

<!-- CSS -->
<!-- CSS -->
<style>
    .promo-banner {
        background-color: #ff5733; /* لون الخلفية */
        color: white; /* لون النص */
        padding: 15px; /* حشوة */
        text-align: center; /* محاذاة النص */
        position: fixed; /* يجعل الشريط ثابتًا في أعلى الصفحة */
        top: 0; /* الموضع من الأعلى */
        left: 0; /* الموضع من اليسار */
        right: 0; /* الموضع من اليمين */
        z-index: 1000; /* لضمان ظهور الشريط فوق كل العناصر */
        overflow: hidden; /* لإخفاء المحتوى الزائد */
        display: flex; /* لجعل المحتوى في سطر واحد */
        justify-content: space-between; /* توزيع المسافات */
        align-items: center; /* محاذاة العمود */
    }

    .banner-content {
        display: flex;
        animation: slide 10s linear infinite; /* تأثير الحركة */
        white-space: nowrap; /* لمنع التفاف النص */
    }

    .promo-text {
        margin-right: 20px; /* مسافة بين النص والزر */
    }

    .close-button {
        background: none; /* إزالة الخلفية */
        border: none; /* إزالة الحدود */
        color: white; /* لون النص */
        font-size: 20px; /* حجم الخط */
        cursor: pointer; /* شكل المؤشر */
        margin-left: 20px; /* مسافة بين النص وزر الإغلاق */
    }

    @keyframes slide {
        0% {
            transform: translateX(100%); /* يبدأ من خارج الشاشة من اليمين */
        }
        100% {
            transform: translateX(-100%); /* ينتهي خارج الشاشة من اليسار */
        }
    }

    .promo-button {
        background-color: #ffffff; /* لون الزر */
        color: #ff5733; /* لون النص */
        padding: 10px 20px; /* حشوة الزر */
        border: none; /* إزالة الحدود */
        border-radius: 5px; /* زوايا دائرية */
        text-decoration: none; /* إزالة الخط السفلي */
        transition: background-color 0.3s; /* تأثير عند المرور */
    }

    .promo-button:hover {
        background-color: #f0f0f0; /* تغيير لون الخلفية عند المرور */
    }

 


    
</style>

<!-- JavaScript -->
<script>
    function closeBanner() {
        document.getElementById('promoBanner').style.display = 'none'; // إخفاء الشريط
    }




        // إظهار الزر عند التمرير
window.onscroll = function() {
    const button = document.getElementById("scrollToTop");
    if (document.body.scrollTop > 100 || document.documentElement.scrollTop > 100) {
        button.style.display = "flex"; // إظهار الزر
    } else {
        button.style.display = "none"; // إخفاء الزر
    }
};




</script>